# Reviewer comparison — kimi vs gemini on the web-playground effort

Continues the cleanup's ledger (`coordination/cleanup-2026-09/reviewer-comparison.md`), same
columns, so the default-reviewer decision stays a number. "Real" = a finding adjudicated
ACCEPT that changed the plan or the diff; "false" = REBUTTED because the claim was wrong on
inspection; "policy" = rebutted only because it conflicted with a standing ruling. Time is the
review file's mtime minus the dispatch timestamp.

| subject | reviewer | findings | real | false | policy | notable | time |
|---|---|---|---|---|---|---|---|
| plan v1 | kimi | 13 | 13 | 0 | 0 | overlay flushed only on scene save (script edits lost on reload); imported slug absent from the build-time project list; dirty guard on project switch; quota failure must not leave "Scene saved" standing | ~9 min |
| plan v1 | gemini | 8 | 6 | 2 | 0 | Rhai script functions cannot read outer-scope variables, so Scope-seeded params could never work (unique); post-physics-only scripts lag kinematic paddles a frame (unique); commands lacked entity targets; false: winit key leak (canvas-scoped listener), request ids over a FIFO | ~5 min |
| plan v2 | kimi | 9 | 9 | 0 | 0 | revision check is get-then-put, not a CAS (two tabs at 3 both write 4); base-joined VFS keys vs the relative project root — a save lands on a different key and the editor keeps reading the stale bundled copy (unique); `[profile.wasm-release]` ignored outside the workspace root (unique); dropped queue lines break FIFO pairing (unique) | ~10 min |
| plan v2 | gemini | 10 | 10 | 0 | 0 | Rhai `call_fn` passes by value so a plain ScriptCommands loses every command (unique, Critical); IDB unavailable in private browsing aborts boot (unique); compile on save so Edit mode shows errors (unique); shared with kimi: CAS, debounce loss, key mismatch, non-atomic import | ~4.5 min |
| plan v3 | kimi | 8 | 7 | 1 | 0 | `save_preferences` only runs from `on_exit`, which the wasm loop never calls — verified against app_handler.rs (shared with gemini); a tab races itself on quick double saves (shared); bundled copy target ambiguous by one `assets/` level (unique); Reset gated on version mismatch hides a same-version import (unique); content hash so a fix without a version bump surfaces (unique); false: doubted zip 4.6.1 exists | ~13 min |
| plan v3 | gemini | 9 | 8 | 1 | 0 | IndexedDB transactions go inactive across an await, so the CAS must chain inside the callback (unique, Critical); Rhai never coerces INT to FLOAT (unique); blank console lines skip the response and desync the FIFO — cited api.rs:54-56 (unique); false: contact points for paddle deflection (pong never reads contacts; the capsule does it) | ~4.5 min |
| batch 1 (gemini authored) | kimi | 3 | 1 | 0 | 2 | checked every caller of the changed functions; real: native/wasm divergence of the new `remove_prefix`/`list_files` on a file or empty prefix; policy rebuts: never-follow-symlinks (kimi's own round-1 F10, Jesse's ruling) and save_store's documented symlink replacement | ~9 min |
| batch 1 (gemini authored) | claude | 4 | 4 | 0 | 0 | magic number 6 where a constant was deleted; silent prefs-load failures; the lost "absent prefs start from defaults" test; ran the deletion grep gemini omitted (`SceneIoError::CreateDirectory`) | — |
| plan v4 | kimi | 5 | 5 | 0 | 0 | `put` never writes a manifest so the v4 orphan sweep deletes every save of a bundled project (Critical, shared); `EditorGame` must forward the new defaulted `register_scripts` or every game under the editor registers nothing (unique); a genuine cross-tab StaleRevision had no keep-my-version path and its message induced the loss (unique) | ~7 min |
| plan v4 | gemini | 8 | 8 | 0 | 0 | same sweep Critical; a view shared by all instances cannot carry "the owning entity" and one-view-per-frame contradicts two phases (unique, two findings); empty blackboard deadlocks pong's serve (unique); switch cleared queued saves (unique); untitled scene saves to a relative path on the web — cited scene_io.rs:61/api.rs:172 (unique); `This` is not a Rhai identifier (unique) | ~6 min |
| plan v5 | kimi | 10 | 10 | 0 | 0 | batch 5's import text contradicted the drain-then-epoch rule the same plan states (shared with gemini); batch 3 named batch 7 APIs and could not build as written (unique); `build_wasm.sh:63` reads a per-crate Cargo.lock a member lacks (unique); pagehide re-issue duplicated queued puts into a false conflict (unique); `me.transform.x = 5` silently no-ops unless getters-only (unique) | ~10 min |
| plan v5 | gemini | 10 | 9 | 0 | 0 | boot never seeded base revisions so the first save after reload false-conflicts (Critical, unique); `.rhai` paths relative on the web never resolve (Critical, unique); `centre` undefined and restart racing the serve (unique); export needs the manifest; apply needs dt; output nesting vs ASSET_BASE (unique); partial: math package (StandardPackage already has it) but no Vec2 type existed | ~8 min |
| plan v6 | kimi | 9 | 9 | 0 | 0 | Rhai scripts have no defaults channel — a picker-attached script errors on frame one until every param is typed (unique, Major); import validates sidecars but not scenes, so one typo bricks an imported-only project with no recovery (unique, Major); the chain's failure state was undefined (unique); epoch must bump at the START of drain; partial command buffer on error; `check_source` is syntax-only; LEFT WINS hardcoded | ~11 min |
| plan v6 | gemini | 7 | 7 | 0 | 0 | HashMap instance order races the goal's reset against the ball's speed maintenance in one phase (unique, Critical); `build_wasm.sh` looks for the wasm in a per-crate target dir a workspace member lacks and hardcodes the `games/` sync path (unique); queued bytes skipped on tab close; a conflicted path retried forever; textarea edits invisible to the dirty check (unique); backslash zip paths; zero-vector normalize | ~5 min |
| batch 2 (gemini authored) | kimi | 4 | 4 | 0 | 0 | flagged the dropped platformer fallback as an unauthorised behaviour change and that the prior round's mandated doc was missing (shared with Claude); named-entities map stale for entities created mid-Play via the command API (unique); swallowed physics init error; dead getter | ~7 min |
| batch 2 (gemini authored) | claude | 4 | 4 | 0 | 0 | the fallback ruling with the mechanism (velocity on a body-less entity goes to a rapier body that never exists); dead getter; `test_` prefix convention (67 of 73); swallowed init error | — |
| batch 3 plan correction (Claude authored) | kimi | 5 | 5 | 0 | 0 | `prefs_slot` wired on the save side only — the load side kept reading `EDITOR_PREFS_PATH`, so the web would persist every session and restore nothing (unique, Major); the upserted manifest copies the bundled hash, so a hash-gated Reset never appears after a plain edit (unique, Major); a fresh write to a conflicted path unspecified; strip failure and the manifest's source unspecified; frame-counted settle | ~7 min |
| batch 3 plan correction (Claude authored) | gemini | 6 | 6 | 0 | 0 | `is_pending` = "not idle" makes a conflicted or stranded path block `drain_then_epoch` forever — no switch, no reset (unique, Critical); the settle rule during Play captures the game camera over the authored one (unique, Major); explicit relative `save <path>` bypasses the base join and never persists (unique); headless drops the base it was given; `write_string` double-notifies; `StoredFile.content_hash` needs a hashing crate the manifest lists nowhere | ~6 min |
| batch 3 (gemini authored) | kimi | 6 | 4 | 2 | 0 | nothing on wasm drives `Chains::poll_all` — only the first write per path reaches IndexedDB, the dirty flag sticks, every switch/reset times out at 5 s (shared with Claude, Critical); the epoch gate's sole caller passes the current epoch (shared); the doc's bridge names do not exist and the cap reads 256 for 1024 (shared); `expect()` in IDB callbacks aborts the module (shared); false: native editor resolves against launch cwd (ProjectHost sets the base before capture), `\u{ffff}` bound hides non-BMP names (code-unit order) | ~6 min |
| batch 3 (gemini authored) | claude | 18 | 17 | 1 | 0 | the transaction adapter's abort handler never reads the CAS result cell, so a real `StaleRevision` lands as `Backend` → STRANDED and retried instead of CONFLICTED (unique, Major); pagehide re-puts queued bytes with the stale base (unique, Major); eight of the section's tests missing; ~150-site abbreviation sweep; `Send + Sync` bound forcing two `unsafe impl`s; directory double's replace not marker-atomic; own m17 (early `script_errors`) withdrawn | — |
| batch 3 round 2 (gemini fixes) | kimi | 6 | 6 | 0 | 0 | the round-1 fix prescription was itself the defect: with `onabort` preferring the external cell, a put callback's early `Ok` survives a quota abort and resolves as success — silent data loss with the dirty flag cleared (unique, Critical); `strip_prefix(root)` is a string prefix, so `pong2/x` lands under `pong` as `2/x` (unique, Major); `.rhai` persisted before the check refuses it; a failed prefs write silences retries; the lost boot-mismatch hint; manifest-less files never cleaned | ~6 min |
| batch 3 round 2 (gemini fixes) | claude | 4 | 4 | 0 | 0 | tests that press Play write `editor_prefs.json` into the crate directory (invisible: gitignored); a cursor re-fetching the value it already holds; naming leftovers; a stale test name | — |
| batch 3 round 3 (planner fixes) | kimi | 7 | 5 | 1 | 1 | an absolute-path save outside the project root persists nowhere and only logs — the one silent path among bannered ones (unique, Major); `manifests()` swallows failures into "nothing stored"; a hitched half-second frame commits a mid-drag camera; the test-support slot shared across parallel tests; no migration note for relative `save`; false: the `\u{ffff}` bound again (UTF-16 code-unit order); policy: clearing `draining` on success would let a post-drain write resurrect a removed project | ~7 min |
| batch 4 section correction (planner authored, plan diff in code mode) | kimi | 3 | 2 | 1 | 0 | the engine `.gitignore` has no `dist/`, so the playground build output sits untracked beside the crate (unique, Minor); Reset has no confirm; batch 5's download control would read a binary conflicted file through `read_to_string` (unique, speculative); false: "no code path produces `origin: saved`" — the three stores' upsert branch sets it (`store/indexed_db/mod.rs:329`, `memory.rs:76`, `directory.rs:130`), so the "changed since you saved" note is live | ~4 min |
| batch 4 section correction (planner authored, plan diff in code mode) | gemini | 4 | 4 | 0 | 0 | `await init()` resolves before the spawned boot loads the manifests or installs the bridge, so a select populated right after it is empty forever and the console answers "busy" (unique, Critical); the script's cargo queries answer for the cwd, not `$GAME_DIR` (unique, Major); "focus the canvas after the switch" is unreachable code past a navigation; the page has no source for the loaded slug on a cancelled switch | ~5 min |
| batch 4 engine diff (gemini authored) | kimi | 4 | 4 | 0 | 0 | the new version hard-fail greps match the entry's doc comment, not the `pub const` lines, so a stale header passes a real bump and `--sync` ships a bundle whose compiled-in base 404s after deploy (unique, Major); `--kind playground` with no `--project` builds a silently empty bundle; `! -name manifest.json` drops a project's own manifest at any depth; the `--sync` immutability warning was deleted (shared with Claude) | ~5 min |
| batch 4 site diff (gemini authored) | kimi | 3 | 2 | 1 | 0 | the 50 ms response poll starts before `wasm.default()` resolves, and every glue export dereferences the still-undefined module, so it throws an uncaught TypeError per tick for the whole download (unique, sharper than Claude's "poll too early"); no build gate resolves the embed's `data-wasm-src` the way rule 3 resolves frontmatter (unique); false: "the select may diverge from the open project" — `web_entry.rs:125-147` reads `?project=` and defaults to the first bundled slug, which is the page's fallback | ~7 min |
| batch 4 both diffs (gemini authored) | claude | 11 | 10 | 0 | 0 | the shortcut list says Space/P plays — Space pans, play is Ctrl+P, stop Ctrl+Shift+P, and a dozen bindings a keyboard user needs are missing (unique, Major); a confirmed switch or reset prompts twice because the page's own `beforeunload` re-reads the dirty flag after the confirm (unique, Major); the test page written twice in the script; four reason comments dropped (shared); a CSS token that does not exist plus hard-coded colours bypassing the high-contrast variant; heading skip; S5 superseded by kimi's sharper version | — |
| batch 4 engine round 2 (planner fixes) | kimi | 2 | 2 | 0 | 0 | the zero-project guard's message described a working build while refusing it, never the reason (unique); the anchored diagnostic goes blank for a const declared in any other form — keep the strict gate, loosen only the extraction (unique); five verified-OK items listed, including that the unified test page is equivalent to the two it replaced | ~7 min |
| batch 4 site round 2 (planner fixes) | kimi | 3 | 2 | 0 | 1 | a second `playground-ready` would stack poll intervals — guard on a stored handle (unique); the embed-path gate sees only `dist/`, so the README says assets land before the `src` moves (unique); policy: resetting `leavingByChoice` in the ready handler would be dead code — the reload restarts the script, and the event never fires twice in one document | ~7 min |
| batch 5 section correction (planner authored, plan diff in code mode) | kimi | 5 | 5 | 0 | 0 | an import that round-trips `project.ron`'s bundled `content_hash` leaves `differs_from_bundle` false, so the "you imported over the bundled project" note the plan's own browser check names can never show (shared with gemini, Major); `reissue_stranded` ignores `draining`, so a tab-hide during the import window re-puts stale bytes with a stale base over the fresh import (unique, Minor); the glue's type cast not extended; the poll is 100 ms not 50; "exactly as it was" overstates the failed-import contract | ~13 min |
| batch 5 section correction (planner authored, plan diff in code mode) | gemini | 7 | 7 | 0 | 0 | export walked the whole root and then added `project.ron` and `README.md` again, so any tree holding them yields duplicate entries the importer refuses — exports could not be re-imported (unique, Critical); an `active_store()` `None` after the drain leaves `draining` set and freezes every save until reload (unique, Major); a file input not cleared after a rejection fires no `change` for the same fixed file (unique, Major); a Blob built when the conflicted link rendered is stale by the time it is clicked (unique, Major); `zip -r .` writes `./` prefixes; an `<a>` cannot be `disabled`. Run ended in a stream interruption after the text was produced; recovered from the kept ndjson | ~8 min (488 s reported) |
| batch 5 engine diff (gemini authored) | kimi | 5 | 3 | 1 | 1 | export has no size cap while import refuses over 64 MiB, so a large export can never be re-imported (unique, Minor); mid-path `./` and `//` survive normalisation and become unreachable stored keys (unique, Minor); the duplicate check runs before the README skip; doc line: a successful import requires the reload, which the page does but the table never said (Major as filed, accepted as the sentence); false: an empty imported `content_hash` misreports against an empty bundled hash — the deployed `projects.json` always carries a sha256; the empty one is the parse-failure fallback | ~12 min |
| batch 5 site diff (gemini authored) | kimi | 4 | 4 | 0 | 0 | revoking the blob URL in the click's own task can silently drop the download in Firefox (unique); switch, reset, export and a second import stay enabled during an import's drain-and-replace window and race it (unique); the picked file is buffered before the 64 MiB refusal can act (unique); two conflicted files sharing a basename get identical buttons (unique) | ~7 min |
| batch 5 both diffs (gemini authored) | claude | 5 | 5 | 0 | 0 | the path rule landed twice — `validate_bridge_path` kept its loop and called the new helper that repeats it (unique); narration comments in `archive.rs`; the site roadmap still said export and import follow (unique, stale doc); the conflicted-paths test set the state by hand instead of driving `handle_put_completion` (unique) | — |
| batch 5 engine round 2 (planner fixes) | kimi | 3 | 1 | 1 | 1 | export mirrors only the archive-size cap, not the decompressed-bytes cap, so a highly compressible project over 64 MiB unpacked exports but cannot re-import (unique, Minor; accepted as wording, parity filed); false: `to_str()` dropping non-UTF-8 segments — zip 4.6.1's `name()` is already a decoded `&str`; policy: duplicate READMEs accepted is what its own round-1 F5 asked for | ~9 min |
| batch 5 site round 2 (planner fixes) | kimi | 4 | 1 | 2 | 1 | the mirrored 64 MiB constant has no visible coupling to the engine's (unique, Minor; a comment names it); false: the engine may expand a compressed archive unboundedly — `MAX_DECOMPRESSED_BYTES` caps it as entries are read, with a 70 MiB-of-zeros test; false: a same-slug `location.search` assignment may not navigate — its own review-20 verified it reloads; policy: the transition guard lists every page-side mutating control | ~3 min |
| batch 6 section correction (planner authored, plan diff in code mode) | kimi | 5 | 5 | 0 | 0 | hierarchy row clicks have no `suppresses_click()` guard, so a `.rhai` drop's release frame also fires the row click and the modifier mode perturbs the selection (unique, Major); `dragging_payload()` is `None` on the release frame, so the section's "while a Script payload is in flight" gate could never consume the drop (unique); the "mod.rs takes no new code" claim was false (shared with Claude); pseudo-rows under a collapsed entity unspecified (unique) | ~5 min |
| batch 6 section correction (planner authored, plan diff in code mode) | claude | 7 | 7 | 0 | 0 | `header` records `current_y` AFTER `component_header` advances it, so the scroll target would have been the first field row, hiding the block title; `history.execute` pushes without `try_merge`, so both drop cases take it and every drop is its own undo entry; blank `ide_command`; `HierarchyClick` derives; the `scroll_to` test | — |
| batch 6 section correction (planner authored, plan diff in code mode) | gemini | 9 | 9 | 0 | 0 | eight shared with kimi or Claude; unique: an `Open source` button on a ref whose `source_path` is empty resolves `""` against `asset_base` and opens the IDE on the asset root — render it only for a non-empty path | ~3 min |
| batch 6 engine diff (gemini authored) | kimi | 5 | 3 | 0 | 2 | the asset browser refuses to arm a drag while Playing and the viewport refuses drops, but Play never cancels an in-flight drag and the hierarchy's new drop path has no play-state guard — a drag straddling the Play keypress lands on the live world and Stop erases the attachment the status bar confirmed (unique, Major); no duplicate guard on a second drop of the same file (unique); a hand-edited `ide_command` is rewritten away by the next autosave (unique, doc'd + issue); policy rebuts: `.rs` tiles inert and script-row clicks ignoring modifiers are both what the section rules | ~10 min |
| batch 6 engine diff (gemini authored) | claude | 3 | 3 | 0 | 0 | the report misdescribed two behaviours the code got right (collapse, label) — the diff, not the prose, is what lands; the spawned IDE child is never reaped (launchers fork and exit, one zombie per click); macro named `"drop_script"` where every other macro is human-readable; ran the native gates the executor's report claimed — they failed until `PKG_CONFIG_PATH` matched the login shell | — |
| batch 6 engine round 2 (planner fixes) | kimi | 3 | 0 | 0 | 3 | the Paused state still accepts the drop the fix guards against — true, and the standing Paused-is-editable rule (audit §1.5, now filed) rather than this batch's hole; its "at minimum" ask, the test comment, was taken; one reaper thread per `--wait` editor open (by design, bounded by open editors); byte-equality on `source_path` for idempotency (speculative) | ~2.5 min |
| batch 7 section correction (planner authored, plan diff in code mode) | kimi | 6 | 5 | 1 | 0 | `reset(asset_base)` cannot clear a `Blackboard` World resource — `World::clear` leaves resources alone and the pinned signature had no world (shared with gemini, Major); the catalog rebuild's only call site was inside `is_playing()`, so a Rescan while Editing showed a stale picker (shared); the native status bar wired to a mirror that is `None` natively (shared with Claude); a Paused `Scripts` edit shifts `(EntityId, usize)` keys onto the wrong script (unique); `mod.rs` at 590+ has no slack for review rounds (unique); false: "rhai has no `wasm-bindgen` feature" — `cargo info rhai@1.26.0` lists it | ~6.3 min |
| batch 7 section correction (planner authored, plan diff in code mode) | gemini | 7 | 5 | 1 | 1 | an entity despawned in `early_update` still gets an `update` call the same frame because the prune ran once per frame (unique, Critical); `script_picker_open` as a by-value out field is never written back and resets every frame (unique, within its F1); `ParamSpec.name: &'static str` forces a `Box::leak` per parsed `.rhai` header (unique); `engine::rotate` had no rotation verb to issue (unique, half of F4); errors latched after Stop (unique); false: the trait omits `dt` — it is on the shared view by decision; policy: the physics feature gate, which `behavior_runner` already carries and nothing builds without | ~6.1 min |
| batch 7 section correction (planner authored, plan diff in code mode) | claude | 7 | 6 | 1 | 0 | the catalog held on `EditorGame` where the panel hosts (free functions over `EditorContext`) cannot reach it; `ScriptCatalogEntry.params` as a `Vec` the ref would convert for nothing; `errors()` had no stated shape; a per-frame `ensure_scanned` would walk the asset tree per frame; Rhai `INT` is `i64` against `ScriptValue::I32`; retracted: `no_time` over the `wasm-bindgen` hedge — rhai times through `web-time` | — |
| batch 7 engine diff (gemini authored) | kimi | 8 | 7 | 1 | 0 | every Rhai instance re-read its file and recompiled twice a frame, with read failures compiled as an empty AST (shared, Major); `WEB_PLAYGROUND.md` claimed a `.rhai` import check the archive never ran — the check batch 5 had deferred here (unique, Major); the training example would not compile and, registering the engine's own built-in, would panic at startup (unique, Major); the doc promised ongoing-contact queries over a start/stop view (unique); duplicate names resolved in hash order (unique); nothing outside the editor steps the runner and no doc said a game must (accepted as docs; the engine-loop direction rebutted — the game owns the physics step); false: picker widget ids collide for same-stem files — ids are positional, though the label was the id | ~10 min |
| batch 7 engine diff (gemini authored) | claude | 11 | 11 | 0 | 0 | `set_velocity_x("Ball", 250)` zeroed the ball's vertical velocity — the plan's own example (unique); an out-of-range Rhai integer became 0 on the blackboard (unique); two missing target names deduplicated into one error (unique); an unresolved id warned twice a frame and re-read a missing file per phase (unique); the picker's error colour from a freshly built default theme (unique); no `set_sprite_color` binding (unique); four API aliases (unique); recompile per phase and the empty-base join (shared with kimi); the picker rendered its widget id as button text (found while verifying kimi F5) | — |
| batch 7 engine round 2 (planner fixes) | kimi | 3 | 2 | 0 | 1 | `set_velocity_x` then `set_velocity_y` on one entity in one phase: each reads the pre-apply velocity, so the last write carries a stale axis and the no-physics fallback integrates both — the planner's own axis fix, half done (unique, Major); the failed-ref memo is never pruned for dead entities and a script created mid-Play is not retried until the next Play (unique, Minor; prune + doc line); policy: keep the deleted aliases for a deprecation window — they never shipped | ~3.5 min |
| batch 8 section correction (planner authored, plan diff in code mode) | kimi | 5 | 5 | 0 | 0 | the scene pins omit everything that makes the ball bounce — no wall collider, no paddle `RigidBody`, wire-default restitution 0, so "a restitution-0 paddle kills the rally on first contact and the ball script's own guard returns forever" (shared with gemini, Major); test (2) asserts the win in the phase that scores it (shared); `ecs`/`input` are not workspace dependencies (unique); the background is 960 × 720 at −100 (unique); the mod-2¹⁶ hash diverges from `wrapping_mul` after 18 minutes and the `t * 1.2 − 0.6` step was missing (unique) | ~12.7 min |
| batch 8 section correction (planner authored, plan diff in code mode) | gemini | 5 | 4 | 1 | 0 | one shared `<output>` for Save status and the runtime poll — "the poller compares `""` against the current text, detects a change, and overwrites the output with an empty string" within 500 ms (unique, Major); a cancelled switch leaves the select on the new file and the next Save writes the old text under the new path (unique, Major); the physics pins and the win-test lag shared with kimi; false: `has_collision_started(me, other)` needs an `ImmutableString` overload — a Rhai `&str` parameter takes any string value | ~6.2 min |
| batch 8 section correction (planner authored, plan diff in code mode) | claude | 2 | 2 | 0 | 0 | the `Scripts` component's RON wire form was never spelled, though every other component's was — the executor has no example scene carrying one (unique); a wrong line reference for the project select's population (unique); missed what both counterparts caught: the physics defaults and the win test's phase lag | — |
| batch 8 (gemini authored, three repos) | kimi | 3 | 1 | 0 | 1 | traced every new surface against the engine sources first (scene fields, bindings, bridge exports in the rebuilt glue, sensor events, the render-size convention) and reported no regressions; real: "SCRIPTING.md claims the serve heads toward the last scorer's opponent — `last_scorer == "left"` → `dir_x = -1.0`, i.e. the ball heads *toward* the left player — the scorer", which also surfaced that the plan's `"left"` default sent the first serve the opposite way from the Rust game (unique); policy: the ±230 clamp sinks 10 px into the wall — the Rust game's own constant, filed as pong#2; the third is a coverage note (the fallback never runs the speed-maintenance or AI branches), ruled at plan time and taken as a test comment | 9m52s |
| batch 8 (gemini authored, three repos) | claude | 5 | 4 | 0 | 0 | a failed `playground_read_file` leaves the select on a file the panel did not open, so a later Save writes the old text while the control names the new file (unique); dead `empty-cells` CSS; a tag comment in an empty catch; the site README's feature list; verified `sign(f32)`, `abs`, `min`/`max` and the mixed `INT * f32` against the Rhai and engine registries for the branches the test cannot reach (kimi checked the same); note only: Ctrl+Shift+S in the textarea also saves | — |
| batch 9 section correction (planner authored, plan diff in code mode) | kimi | 6 | 4 | 1 | 1 | verified fifteen engine-side pins line by line before writing; real: the shared site components already serve deployed pages and "a regression in the old flows — the refactored population code emptying stored projects, the new prefix branch intercepting a normal project value, or a GameEmbed default-prop mistake — passes every listed gate green" (unique, Major); the wasm target the games gate silently gains (unique); the per-game `cargo test` dropped without a word (unique); false: F5-as-Play reloads the tab — winit's web backend prevents default on canvas key events; policy: the cross-repo pins could not be re-verified from this checkout — they were, in their own checkouts | 8m45s |
| batch 9 section correction (planner authored, plan diff in code mode) | gemini | 6 | 6 | 0 | 0 | the plain wasm build of a game is compiled nowhere but bundle time — "`web_entry.rs` is `cfg(target_arch = \"wasm32\")`, native checks never compile it, and the new check runs only with `--features editor`, so a break in the non-feature branch passes every gate and fails at bundle time" (unique, Major); the script's binary `if/else` ladders would route `--kind editor` into the games `else` and sync an editor bundle over `public/games/<slug>/` (unique, Major); the cancelled dirty navigation leaving the select desynchronised, batch 8's own lesson re-applied (unique); the unconditional feature flag (unique); the diagnostic and the `:59` message (shared / unique) | 8m31s |
| batch 9 section correction (planner authored, plan diff in code mode) | claude | 5 | 5 | 0 | 0 | three line references off by one to two lines; `getCollection` inside a presentational embed where every other component takes props; the diagnostic's wrong constant (shared with both); the wasm target (shared with kimi); Ctrl+S inside a game bundle unspecified — harmless in-memory VFS write, added to the browser check; missed what gemini caught: the plain wasm build nobody compiles and the `else` branches | — |
| batch 9 (Claude Opus 5 low authored, eight repos) | kimi | 3 | 2 | 0 | 1 | verified the editor's wasm path, the slug regex against the `/` sentinel, `leavingByChoice`, both GameEmbed defaults and every non-editor entry byte-for-byte before reporting no regressions; real: "a user on `/playground/pong/` reads the lede — nothing persists — then the shortcuts panel: Ctrl+S saves the scene to this browser" (unique, the copy contradiction on one page; a `saveLine` prop); the 1024/720 clamp as an unguarded three-place contract (unique; the script now greps the engine's constants, per the DRY rule); policy: the games gate hard-requiring the wasm target — its own review-28 F3 asked for exactly that; note: the other five lockfiles — zero diff against HEAD, their native editor checks had resolved the chain in batch 7's wake | 9m00s |
| batch 9 (Claude Opus 5 low authored, eight repos) | claude | 1 | 1 | 0 | 0 | pong's README sentence inserted between a list's lead-in colon and its first bullet (unique); re-ran every gate, rebuilt pong's editor bundle and matched its sha256 to the synced file, diffed the six entries against each other normalised on the game name; the out-of-scope flex fix accepted as the layout gate's own demand; missed kimi's copy contradiction | — |
| batch 10 section correction (planner authored, plan diff in code mode; batch 11 split out) | kimi | 5 | 5 | 0 | 0 | "`// @param spots: int = 4` fails the plan's own `check_source` gate" — verified the header grammar at `param_header.rs:44-141` and pong's `i32` convention; also the README-clobbering `unzip -o`, the `:40` echo, the three-versus-four commits, and the closing comments that named no issue for batches 6–9 | 7m15s (10:32:21 → 10:39:36) |
| batch 10 section correction (planner authored, plan diff in code mode; batch 11 split out) | gemini | 7 | 7 | 0 | 0 | "`game-template` cannot run exported playground projects; `main.scene.ron` and `"coins"` are hardcoded" — the batch's real catch, unique: an export of pong carries its own scene name, so the drop-in the README promises crashed at `init`; also `ecs` not reachable from a game (taken as a prelude re-export), `origin: Imported` refused by `rename_all = "snake_case"`, and the sim test's base path under the games gate | 4m56s (10:32:21 → 10:37:17) |
| batch 10 section correction (planner authored, plan diff in code mode; batch 11 split out) | claude | 9 | 8 | 0 | 0 | the sim test cited native boxes (`lifecycle.rs`) where the Rhai loop is `rhai.rs:18-72`; the 600-line total no honest layout could meet; `set_position` on a static coin; committed `saves/` that need a headed run; two README build commands nobody would have run | — |
| batch 10 (Claude Code authored, four repositories) | kimi | 3 | 3 | 0 | 0 | "the template never applies the scene's `PhysicsSettings` — exports with gravity run with zero gravity": verified against `project_host.rs:63-73` and the Examples scene's `gravity: (0.0, -980.0)`; the headline drop-in would have floated an Examples export on the desktop while its own scene passed because its declared physics equalled the preset; also the coin script's editable divisor and the stale guide line | 7m13s (11:37:02 → 11:44:15) |
| batch 10 (Claude Code authored, four repositories) | claude | 4 | 4 | 0 | 0 | the executor's own finding taken as a gate: no listed gate compiles `src/bin/editor.rs`, so a public item is deletable with everything green and the editor binary broken — the editor-feature clippy joins the ground rules; also the two stale guide lines and the spare image riding into the bundle's content hash | — |
| batch 10 round 2 (planner fixes) | kimi | 2 | 2 | 0 | 0 | "`physics_for(None)` contradicts the parity it advertises — the playground runs no physics for a scene without a block, the desktop invents one": true of the comment, not the shipped flow, and taken as a narrowed comment; the second, the new test's inverted premise (the shipped scene declares a zero-gravity block), likewise a comment | 5m43s (11:47:06 → 11:52:49) |
| batch 11 docs close-out (Claude Code authored, two repositories) | kimi | 2 | 2 | 0 | 0 | "review/rebuttal numbering contradicts the surviving artifacts between batches 5 and 6" — read the three surviving rebuttal headers and found rebuttal 21 claimed by both batches; the second, that "live at beinsiculous.com" was unverifiable from its checkout, was right in fact: `/playground/` was 404 on production at review time and becomes true with this session's merge | 5m56s (13:54:16 → 14:00:12) |
| batch 11 docs close-out (Claude Code authored, two repositories) | gemini | 1 | 1 | 0 | 0 | "the documented export drop-in command silently runs the template's coin demo instead of the exported game" — `unzip -o` adds and does not empty, and `main.scene.ron` sorts ahead of `pong.scene.ron` under discovery; the command gains an `rm` in four documents, one of them the pushed template's README. Unique, and the effort's last real catch | 5m26s (13:54:16 → 13:59:42) |
| batch 11 docs close-out (Claude Code authored, two repositories) | claude | 3 | 3 | 0 | 0 | the archive entry said gemini reviewed every staged diff and executed the effort (it reviewed no diff and executed batches 1–8); a lesson cited `api.rs`, which does not exist; five file suffixes rendered as prose | — |

## Close-out (Sep 6 2026, the effort done)

Totals over the effort, from the rows above: **kimi** 36 reviews, 180 findings, 150 real,
13 false, 16 policy; **gemini** 15 reviews, 104 findings, 97 real, 5 false, 1 policy;
**Claude** (the planner's own read, the same vendor as the author on batches 9–11) 17
reviews, 103 findings, 98 real, 2 false. Kimi read every plan version, every section
correction and every staged diff, including every planner-fix round; gemini read every plan
version and every section correction and no staged diff until batch 11's, which the planner's
own vendor had authored. Wall time held all effort: kimi 3–13 minutes a review, gemini 3–8.

The unique catches split by kind. Kimi's were verification catches — it re-runs the suite,
greps callers across crates and reads the surviving files: the get-then-put revision check
that was not a CAS, the base-joined VFS keys that made a save land on a different key than
the read, the `[profile.wasm-release]` ignored outside the workspace root, every Rhai instance
recompiling twice a frame, the template that never applied the scene's physics block, and the
rebuttal numbering that contradicted the files. Gemini's were design catches on the plan
before a line was typed — Rhai functions cannot read outer-scope variables so the seeded
params could never work, post-physics-only scripts lagging kinematic bodies a frame, the
template that hardcoded its own scene file and so could not run an export, and, on the one
diff it read, the drop-in command that would have played the coin demo instead of the export.
Neither produced a false claim at code level after batch 5; kimi's policy rebuts (16) were all
findings that re-raised a standing ruling, never wrong on the facts.

**Recommendation, as a number:** kimi stays the default single reviewer on a staged diff —
150 real over 180 at code and plan level, with the verification discipline that found the
data-loss class — and **gemini reads every plan and every section correction**, where 97 of
104 were real and the design-level flaws it alone found were the expensive ones. Run both on
a diff whose author is the planner's own vendor (batches 9–11 here), because that is when the
no-model-reviews-its-own-work rule has no other reader. The cleanup's recommendation holds,
with the plan-side half made mandatory rather than advisory.
