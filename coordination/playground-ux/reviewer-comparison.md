# Reviewer comparison — Playground UX

One row per review. "Real" = a finding adjudicated ACCEPT that changed the plan or the
diff; "false" = a finding REBUTTED because the claim was wrong on inspection; "policy" =
rebutted only because it conflicted with a standing ruling. The notable catch is quoted in
the row because the review files are transient. Time is wall clock from launch to the
review file's mtime. The subject names the repo, because review numbering is per subject
directory (`insiculous_2d/review/playground-ux/`, `insiculous_web/review/playground-ux/`).

| subject | reviewer | findings | real | false | policy | notable | time |
|---|---|---|---|---|---|---|---|
| engine · plan v1 | kimi | 11 | 11 | 0 | 0 | "an open command-API batch is silently lost on Keep — rebase only walks history entries"; the merge id-reassignment burying a session id below the floor | ~9.5 min |
| engine · plan v1 | codex | 10 | 10 | 0 | 0 | "Export still omits the edit used to pass the acceptance test" — `playground_export_zip` carries the last saved scene; and the whole-component replay importing simulated fields the user never touched | ~1.6 min |
| engine · plan v2 | kimi | 10 | 10 | 0 | 0 | "the 6 s heartbeat release fires on timer throttling, not just death — silently breaking the one-simulation invariant"; a blocked popup filing the snapshot and latching the reservation | ~7.5 min |
| engine · plan v2 | codex | 7 | 7 | 0 | 0 | "Live Export silently deletes the project's other scenes"; `scripts_to_data` drops a parameter whose target is named only after serialization; the first dispatch died on the usage limit and wrote nothing | ~1.5 min (2nd dispatch) |
| engine · plan v3 | kimi | 7 | 7 | 0 | 0 | "batch 3's pointermove focus handler as specified steals focus from every drag on the page" — drag-selecting console text would focus the canvas; and a reloaded editor page dissolving the preview reservation | ~10.5 min |
| engine · plan v3 | codex | 7 | 7 | 0 | 0 | "Transform2D.position is a glam::Vec2, whose JSON representation is an array" — the leaf-level diff would keep the simulated Y when only X was edited; the snapshot bridge returning bytes without the scene entry the preview needs | ~1.3 min |
| engine · plan v4 | codex | 7 | 7 | 0 | 0 | "calling the existing request_redraw() from a timer still requests an animation frame" — the hidden-tab fallback needed a proxy wake; at 390 px the default dock leaves the centre no width at all | ~2 min |
| engine · plan v5 | codex | 4 | 4 | 0 | 0 | "Keep can recreate an empty entity after simulation destroys a paused creation" — CreateEntityCommand::undo re-captures from the missing entity; and the auto-collapsed inspector that could never reopen | ~2 min |
| engine · batch 1 diff | kimi | 4 | 3 | 0 | 1 | "Cut's undo after Keep respawns the subtree from the paused-world capture" — DeleteTreeCommand captured once at construction; the API write gate under the pending dialog; policy rebut: post-session eviction is the cap doing its job | ~17 min |
| engine · batch 1 diff | codex | 5 | 5 | 0 | 0 | the same two, plus "JSON rebasing cannot preserve removals or enum changes" — a collider shape change deserialized to nothing; and a kept creation replaying its simulated state | ~5 min |
| engine · batch 1 diff | claude | 4 | 2 | 0 | 0 | the dialog's Keep/Discard path skipped the preference save; the heading lost its bold face (two observations, no change) | — |
| engine · batch 1 fix delta | kimi | 4 | 4 | 0 | 0 | "a macro whose children ALL drop reports 1 could not be applied instead of the true count"; the dead discard branch; the scene dialog's identical API gap; plus a list of verified non-issues | ~22 min |
| engine · batch 1 fix delta | codex | 3 | 3 | 0 | 0 | "Enum-switch fix fails when simulation changed the authored variant" — three distinct variants; the bold label measured with the regular face | ~4 min |
| engine · batch 2 diff | kimi | 4 | 4 | 0 | 0 | "narrow tabs and the narrow overlay are placed 24px too high, on top of the toolbar strip" — the scene panel has a header, and the guard test measured from the dock origin; the overlay with no close; the centred clip at 360 px; Escape not closing the overflow menu | ~9 min |
| engine · batch 2 diff | codex | 5 | 5 | 0 | 0 | "Toolbar and narrow panels bypass modal input blocking" — an overlay scope was exempt from every blocking rect whatever its layer, so Play was live behind the Stop dialog; a panel collapsed at desktop width opening as an empty overlay | ~1.3 min |
| engine · batch 2 diff | claude | 3 | 3 | 0 | 0 | the four dead `Toolbar` members; the gizmo clipping to the content rect, strip included (the executor's own flag, folded in) | — |
| engine · batch 2 fix delta | kimi | 2 | 2 | 0 | 0 | "opening a panel as the narrow overlay silently overwrites the user's persisted collapse preference" — `capture_panels` persists the flag and the autosave fires; the empty-viewport `Some` that would leave picking silently dead; plus a cleared list (frame order, layer order, callers) | ~5.7 min |
| engine · batch 2 fix delta | codex | 2 | 2 | 0 | 0 | "Narrow overlays become unusable in short windows" — the planner's own fix had cut the overlay at the centre's bottom instead of the dock's; Escape forwarded to the game while Playing before the cascade could close the overflow menu | ~1 min |
| engine · batch 3 diff | kimi | 3 | 2 | 1 | 0 | "the clicked tile's full-path status message is clobbered by the hover branch from the very next frame"; the pointermove listener collapsing a drag-select of plain page text (the console is an `<output>`); false: the downstream wheel-consumer hazard — no game, example or engine_core path reads the wheel delta (the missing unit in the docs was taken) | ~5.3 min |
| engine · batch 3 diff | codex | 2 | 2 | 0 | 0 | "Hovering an asset silently clears persistent save errors" — `show_message` resets the persistent flag; the one-frame full path | ~1 min |
| engine · batch 3 diff | claude | 2 | 2 | 0 | 0 | the stale "line" test name the executor left for the planner; the tautological clamp comment | — |
| engine · batch 3 fix delta | kimi | 2 | 1 | 0 | 0 | "the click path still erases the error" — the hover guard had made the invariant visible and the click had not caught up; the stale-selection trade-off acknowledged as documented, no change | ~3 min |
| engine · batch 3 fix delta | codex | 0 | 0 | 0 | 0 | no actionable findings: the drag-ghost move, the hover guard and the selection guard cleared | ~0.5 min |
| engine · batch 4 diff | kimi | 6 | 4 | 0 | 2 | "the shared `Completion` is single-consumer, so the documented launch-then-Export join fails every time" — `take` moved the answer out and the test pinned it; the pump arming only on a transition; the export refusal during Play (the doc half taken, the saved-scene fallback rebutted as the silent loss the live export prevents); the controls slot left occupied by a failed `run_game`; policy: the old-page/new-wasm pairing (versioned bundle directories) and the `assets/scenes/` prefix (the section's own rule) | ~25 min |
| engine · batch 4 diff | codex | 3 | 3 | 0 | 0 | "Opening the preview can starve the editor's snapshot request" — the pump was installed on the preview page only, not the editor's tab that the window hides; "Readiness reports running after a fatal graphics failure" — the terminal statuses do not say "failed"; the single-consumer completion | ~1 min |
| engine · batch 4 diff | claude | 5 | 5 | 0 | 0 | the single-consumer completion; the pump on the wrong page; the pump waiting for a transition; the guide's "other four" naming seven; the readiness rule (credited to codex) | — |
| engine · batch 4 fix delta | kimi | 2 | 2 | 0 | 0 | "the eager `pump_while_hidden()` at install is a guaranteed no-op at both new call sites" — the proxy is published by `run_game`, after the installer runs; repeated installs stacking timer chains without a shared armed flag | ~8 min |
| engine · batch 4 fix delta | codex | 1 | 1 | 0 | 0 | "The pump still never starts when the page boots hidden" — the same proxy-ordering gap, half an hour earlier | ~0.5 min |
| site · batch 5 diff | kimi | 6 | 3 | 1 | 2 | "the WebGPU-failure guidance left the live region — the actionable instructions render in the panel with no live role and are never announced"; the Pong fallback still with a Snake alt on five game pages; false: `init()` rejecting after the loop starts (`web_entry.rs` spawns and returns); policy: the dead `#save-status` (batch 11) and the fill-mode `!important` versus the 1024×720 clamp (the engine follows the shown box) | ~11 min |
| site · batch 5 diff | codex | 1 | 1 | 0 | 0 | "Retrying initialization duplicates handlers" — every control listener sat between the import and `wasm.default()`, so a rejected init and a Try again ran one chosen ZIP through `playground_import_zip` twice; "Do not merge" | ~1 min |
| site · batch 5 diff | claude | 5 | 5 | 0 | 0 | the black stage left under the compatibility panel — web#59's own complaint; the panel calling the game page's subject "the editor"; a save refusal outliving the next successful save on the `role="alert"` banner | — |
| site · batch 5 fix delta | kimi | 2 | 2 | 0 | 0 | "the save-refusal banner clears on any JSON response that isn't `{ok: false}`" — tightened to `ok === true`; the wired flag set after the call, so a half-wired throw could be retried into duplicates; plus a cleared list (stage `display`, the `undefined` props, the phone scenario in both gates) | ~6.5 min |
| site · batch 5 fix delta | codex | 1 | 1 | 0 | 0 | "Successful saves leave the 'editor is busy' refusal visible" — the dispatch refusal was written to the banner without being save-owned, so the planner's own clearing fix missed it | ~1 min |
| site · batch 6 diff | kimi | 5 | 3 | 2 | 0 | "the engine is never successfully told a preview is open" — half right: the re-take ran inside `wireControls`, before init and before the bridge hooks exist, so its reservation call was a silent no-op (the fresh-launch half was wrong: `playground_snapshot` reserves as it files); the orphan document's dead Close; adoption closing a cross-origin window. False: an `astro check` strict-null failure the gate had already passed, and a navigation lockout `pagehide` prevents | ~12 min |
| site · batch 6 diff | codex | 2 | 2 | 0 | 0 | "Reload restores the toolbar lock but leaves the engine's Play unlocked" — the same pre-init re-take, found first; and "Preview focus targets the placeholder canvas that the engine removes" — winit replaces `#game-canvas` and keeps the id, so a node captured at boot is detached | ~1 min |
| site · batch 6 diff | claude | 4 | 4 | 0 | 0 | Play ↗ disabled for the whole window lifetime, so the section's reuse ("Play ↗ twice reloads the same window") was unreachable; the preview's Pause flipping its label under `aria-pressed` against batch 5's Fullscreen ruling | — |
| site · batch 6 fix delta | kimi | 2 | 1 | 0 | 1 | "`PREVIEW_OPEN_NOTE` now instructs the user to do the thing they no longer need to do" — the banner text made stale by the reuse fix; policy: a failed relaunch closing the window, kept as the section rules | ~6.5 min |
| site · batch 6 fix delta | codex | 1 | 1 | 0 | 0 | "Relaunching an adopted preview reuses its generation and cancels the launch" — the counter restarts at 0 on reload while the adopted document sits at N, so the old document's `preview-closed` passed the generation check | ~1 min |
| engine · batch 7 section correction (plan doc, 104 lines) | kimi | 3 | 3 | 0 | 0 | "correction 2's 'pre-existing defect' does not exist — the chasers already chase": `update_player_top_down` pushes the `player` tag every frame (`handlers.rs:97`), so the prescribed `EntityTag` was a no-op and its test a false invariant; also the `read_dir` order dependence and two drifted citations | ~7 min |
| engine · batch 7 section correction (plan doc, 104 lines) | codex | 0 | 0 | 0 | 0 | none — "mergeable as a plan update"; its read fence is the engine repo, so the site half (items 6–11) went unverified | <1 min |
| engine · batch 7 diff | kimi | 2 | 2 | 0 | 0 | "the 'every entity has a name' assertion never recurses into `children`" — and the `*.ron` walk would feed a sidecar to the scene parser while the boot-scene claim stayed unguarded; traced the camera through `main_camera_pose`, the editor's Play sync and `first_scene_in` and found no regression | ~6 min |
| engine · batch 7 diff | codex | 0 | 0 | 0 | 0 | none — "Mergeable"; read-only, tests not run | <1 min |
| engine · batch 7 diff | claude | 1 | 1 | 0 | 0 | the camera is entity 0, so the Hierarchy opens with `camera` on its first row while the hint says pick something in it | — |
| site · batch 7 diff | kimi | 2 | 2 | 0 | 0 | "the hint's 'Pick the player' instruction is wrong for every project except the default one" — `/games/`' edit button opens `?project=pong` and the select switches in place; and the hint stays up over the compatibility panel when WebGPU fails | ~7 min |
| site · batch 7 diff | codex | 0 | 0 | 0 | 0 | none — "Mergeable based on static review; browser interaction and layout were not verified"; missed the accent-painted link | <1 min |
| site · batch 7 diff | claude | 2 | 2 | 0 | 0 | the game-page line is never dim: the whole sentence is the anchor and the global anchor rule paints it accent; `.editor-link` restates `.empty` | — |
| site · batch 7 fix delta | kimi | 2 | 2 | 0 | 0 | "the hint is never restored after a successful retry" — the failure path hid it and `bootPlayground`'s retry restores only the stage; and the gate's GPU-less browser now audits a page without the hint, against the component comment's claim | ~5 min |
| site · batch 7 fix delta | codex | 1 | 1 | 0 | 0 | "Successful retry leaves the first-run hint hidden" — the same restore gap, found first | <1 min |
| engine · batch 8 diff (docs) | kimi | 2 | 1 | 0 | 1 | "the roadmap still lists the audit reconciliation as pending Playground-UX scope, contradicting the diff's own 'reconciled' claim" — the sprint paragraph's clause, now marked done; policy: unpushed hashes dangle only under a rebase the plan never does; verified every "open" mark against the tree | ~4 min |
| engine · batch 8 diff (docs) | codex | 3 | 3 | 0 | 0 | "§3.6 marked shipped despite remaining read-only components — all four remain `{ readonly }` in `stored_component/mod.rs`"; also §1.2's "no component type is dropped" against the unregistered-type test, and the eleven "no issue" items with no destination (the filing the batch had planned) | <1 min |
| engine · batch 8 diff (docs) | claude | 4 | 4 | 0 | 0 | the archive entry's byline: "Astra's file:line-anchored read" — the adopting commit names no author, the roster postdates the audit, and #123 quotes Astra calling it "your older UX audit"; also §3.6 and §1.2 (with codex) and the "82 marked items" count | — |
| engine · batch 9 section correction (plan doc, 99 lines) | kimi | 5 | 5 | 0 | 0 | "collapsing by skipping the editor fn draws no header for thirteen arms — collapse becomes irreversible data-hiding, not a toggle" (`inspector.header("Sprite")` is inside the editor fn); also `action_button` and the script picker outside the read-only flag, `resume_from_pause` as the second way into Playing, and two incomplete lists | ~7.5 min |
| engine · batch 9 section correction (plan doc, 99 lines) | codex | 2 | 2 | 0 | 0 | "replacing color controls with ordinary display rows preserves the scroll bug" — a display row advances `row_height` where the colour row advances `color_block_height`, and the Play path drew no add-component section, so the content shrinks and the offset clamps; also `action_button` and the arm's direct `remove_button` | <1 min |
| engine · batch 9 diff | kimi | 4 | 4 | 0 | 0 | "the colour editor's target is keyed by walk-time `component_index`, so a structural change while it is open silently retargets it" — an undo that removes a component moves the popup to whatever colour row now sits at that index; also the silent hex rejection, the missing x clamp, and the value-conditional section during Play (accepted as a documented limit); verified every gate on the read-only path and found no live mutation | ~11 min |
| engine · batch 9 diff | codex | 3 | 3 | 0 | 0 | "an upward-opening colour popup edits fields underneath it" — the popup pushes its blocking rect from inside the row, after the rows above it have interacted this frame; also no horizontal clamp, and the nested overlay inside the narrow-mode panel that drops the scope for every row after the colour row | <1 min |
| engine · batch 9 diff | claude | 4 | 4 | 0 | 0 | "cancelling a rename on entering Playing leaves the keyboard captured" — the UI keeps an undrawn widget's focus, nothing calls `clear_focus`, and `route_editor_key` consumes every key before the Playing branch; the popup's three placement findings shared with codex, traced to one cause and one reshape | — |
| engine · batch 9 fix delta | kimi | 4 | 4 | 0 | 0 | "a rename scrolled off-panel still swallows the keyboard — the new release rule doesn't cover it": `render_node` culls off-panel rows, so the field goes undrawn with `renaming` unchanged and nothing can take the Escape; also two "next frame" docs, a test asserting against the wrong bounds, and the hex field's focus surviving a click-outside close | ~10 min |
| engine · batch 9 fix delta | codex | 2 | 2 | 0 | 1 | "closing the popup discards click-away commits" — the pass recorded the edit and closed in the same call, taking the pending value with it; policy: the scrub-through under the popup is `float_scrub` arming from raw mouse state, filed as 2d#134 from batch 2 and kept out of a batch that does not touch `crates/ui` | ~1 min |
| engine · batch 9 fix delta | claude | 1 | 1 | 0 | 0 | the pass ran after the toolbar strip, so a popup flipped over the strip left its buttons live; moved to the top of the frame | — |
| engine · batch 9 fix delta 2 | kimi | 5 | 5 | 0 | 0 | "the deferred close's global `clear_text_focus()` steals focus from whatever field the click-away landed on" — the fix for the stuck keyboard broke the click-from-popup-into-a-field flow; also the entity-switch drop of a click-away commit (scoped to the editor backlog #99), the scrim drawing over the popup, a transient cull ending a rename, and the other close paths not releasing focus | ~8 min |
| engine · batch 9 fix delta 2 | codex | 2 | 2 | 0 | 0 | "closing the colour popup discards an unrelated rename" — the global focus clear cancelled a click-away rename commit; and the pass ahead of the confirm dialogs let a popup channel take the presses meant for their buttons | ~1.5 min |
| engine · batch 9 fix delta 3 | kimi | 3 | 3 | 0 | 0 | "a hierarchy resting at zero visible rows now traps the keyboard in an undrawn rename field indefinitely" — the unconditional empty-pass grace, now bounded to three passes; also the script-row viewport (with codex) and the dialog discarding the popup, accepted as the documented trade | ~7 min |
| engine · batch 9 fix delta 3 | codex | 1 | 1 | 0 | 0 | "script-only hierarchy views leave an invisible rename holding keyboard focus" — `rows_drawn` counted entity rows only, so an entity whose script rows filled the panel read as "drew nothing" | ~1 min |
| engine · batch 9 fix delta 4 | kimi | 2 | 2 | 0 | 0 | "the fix never runs for a hidden panel — the trap it claims to close still exists on that path" (with codex; kimi saw the fix already in the tree); and the three-frame grace being wall-clock-blind, so a paused drag through zero cancelled the rename — widened to thirty passes | ~5.5 min |
| engine · batch 9 fix delta 4 | codex | 1 | 1 | 0 | 0 | "hidden panels never advance the rename timeout" — a narrow-mode tab or a hidden panel never renders, so the settlement never ran; the frame now runs it when the dock leaves the hierarchy out | ~1 min |
| engine · batch 9 fix delta 5 | codex | 0 | 0 | 0 | 0 | none — "Mergeable"; static, tests not run | ~1 min |
| engine · batch 9 fix delta 5 | kimi | 2 | 2 | 0 | 0 | "F2 starts a rename the new settle is guaranteed to kill" — with the hierarchy not rendered, the shortcut opens a field that never draws and ends after the grace; and hiding the panel mid-rename now drops the typed text silently (the intended semantic, minus an affordance) — both to the editor backlog #99 | ~4.5 min |
| engine · batch 9 test split (tests only) | codex | 0 | 0 | 0 | 0 | none — "Mergeable based on static review" | <1 min |
| engine · batch 9 test split (tests only) | kimi | 1 | 1 | 0 | 0 | "`inspector_tests.rs` module doc still claims the colour-editor contracts live there" — trimmed | ~4 min |
| engine · batch 10 plan correction (the re-verification paragraph, 10a/10b split) | codex | 3 | 3 | 0 | 0 | "Camera bounds do not match the resized preview" — the web boot follows the canvas's shown box and the preview stretches its canvas to its container, so the overlay was re-scoped to the game frame at its configured size; also the tooltip drawing above a modal from a covered control (blocking test shared with `interact`) and the delay never restarting for movement inside the anchor (rest to show) | ~1 min |
| engine · batch 10 plan correction (the re-verification paragraph, 10a/10b split) | kimi | 3 | 3 | 0 | 0 | "the existing `shed.is_empty()` early-return silently kills the new overflow-menu case the plan depends on" — the guard goes and the menu returns an `OverflowPick`; the same preview-size premise as codex; and `FIELD_HEIGHT` as a free const decoupling inputs from the runtime `style.row_height` (a `field_height()` on the style instead); verified the strip and narrow-mode arithmetic, the prefs flatten, winit 0.30's `set_cursor` on web, the caller inventory and the ceilings | ~14 min |
| engine · batch 10a (quieter overlays, View toggles, the game frame, the strip's right group) | codex | 1 | 1 | 0 | 0 | "Expanded overflow menu puts controls outside short browser viewports" — the menu took the button's bottom with no window clamp; it now shifts up to stay inside the window, with a test at a 240 px canvas | ~1.5 min |
| engine · batch 10a (quieter overlays, View toggles, the game frame, the strip's right group) | kimi | 4 | 2 | 1 | 1 | "`Toolbar::render_overflow_menu` is now orphaned dead code with divergent behavior" — deleted with its bounds fn and consts; the vacuous play-controls test (with the planner); false: presses in the menu's padding falling through — the menu's overlay scope registers the whole rect as blocking; policy: hiding the frame while Playing — the review-25 ruling makes the frame the configured size in every state | ~9 min |
| engine · batch 10a (quieter overlays, View toggles, the game frame, the strip's right group) | claude (planner) | 5 | 5 | 0 | 0 | "the 'toggle off / no camera draws nothing' test never calls the gate it claims to test" — it reconstructed the caller's `if` in the test; the scene view now takes the UI and the world and a contract test renders it through the real path; also the report claiming the frame defaults off while the code defaults on | — |
| engine · batch 10a fix delta 1 | codex | 1 | 1 | 0 | 0 | "Shifted overflow rows send clicks to the toolbar underneath" — the window clamp let the menu overlap the strip, whose widgets were processed before the menu registered its blocking rect; the menu now renders first, with a two-layer click test | ~1.5 min |
| engine · batch 10a fix delta 1 | kimi | 1 | 1 | 0 | 0 | "a fully shifted-up overflow menu can overlap the top menu bar, which renders before the menu's blocking rect exists" — the clamp's floor was zero; it is now the strip's top, carried on `StripLayout` | ~11 min |
| engine · batch 10a fix delta 2 | codex | 1 | 0 | 0 | 1 | "The new floor makes previously reachable menu entries fall outside short canvases" — the mirror of kimi's review-27 finding; rebutted with the geometry: the case needs a canvas both narrower than 391 px and shorter than ~230 px, and the native window is clamped to 1024×720 | ~1.5 min |
| engine · batch 10a fix delta 2 | kimi | 3 | 0 | 1 | 2 | "with the menu shifted over the strip, clicking the overflow button picks the first row" — the row is drawn over the button, so the click goes to what is visible; also the widened fall-off band (the recorded trade) and a one-frame opening delay (cosmetic) | ~8 min |
| engine · batch 10b (the tooltip widget, the resize cursor, the layout tokens) | codex | 3 | 2 | 0 | 1 | "Crossing the toolbar prematurely commits an active scrub" — the executor's new float-scrub blocking test ran on every frame of the gesture, and the strip's chrome rect is registered before the panels draw; the test now runs on the arming press only. Also the header/chevron ping-pong (with the planner). Rebutted on scope: the wider-than-window tooltip (filed as #142) | ~1 min |
| engine · batch 10b (the tooltip widget, the resize cursor, the layout tokens) | kimi | 3 | 2 | 0 | 1 | "tooltips raise while a mouse button is held, not just on the press frame" — the claim is now gated on `mouse_down` (with the planner); the seal-vs-cancel asymmetry it named dissolved with the arm-only fix. Its "verified clean" list said the chevron wins over its header — the opposite of the code. Rebutted on scope: the wider-than-window tooltip (#142) | ~11 min |
| engine · batch 10b (the tooltip widget, the resize cursor, the layout tokens) | claude (planner) | 2 | 2 | 0 | 0 | "two nested anchors under the pointer flip the tooltip every frame, so a chevron never shows one" — the header band and its chevron each took up the other's anchor at zero; the frame's anchor is now settled once at `end_frame`, smallest live anchor first (codex found the same). Also the held-press re-raise (with kimi) | — |
| engine · batch 10b fix delta 1 | codex | 2 | 2 | 0 | 0 | "A covered anchor can suppress the overlay's tooltip" — the candidate was picked by area at call time, so an overlay opening after a smaller widget it covers left the covered one to be blocked at end_frame; every anchor under the pointer is now kept and the smallest live one chosen against the complete regions. Also the click that begins and ends inside one frame (the press edge without the held flag) | ~23 min |
| engine · batch 10b fix delta 1 | kimi | 2 | 1 | 1 | 0 | "an armed scrub now also survives a modal scrim — the old force-seal on cover is gone" — accepted as the rule and written down: a gesture is the field's until release or Escape, whatever opens over it. Rebutted: the stale scrub a missed release leaves behind cannot exist — unseen widget state is pruned at end_frame; a test written to the scenario passed on the reviewed code | ~32 min |
| engine · batch 10b fix delta 2 | codex | 0 | 0 | 0 | 0 | "No actionable findings" — the candidates filtered against the complete regions, the same-frame click resetting the delay | ~1 min |
| engine · batch 10b fix delta 2 | kimi | 2 | 1 | 0 | 1 | "equal-area nested anchors now resolve last-drawn-wins instead of first-drawn-wins" — no caller has two, and last-drawn is the inner widget; the tie-break is now in the doc comment. The per-hovered-anchor String allocation is accepted: two small allocations a frame while the pointer rests, against a reused-buffer scheme the settle-at-end-of-frame design made awkward | ~7 min |
| engine · batch 11 diff (save state) | codex | 2 | 1 | 0 | 0 | "Memory-only saves are reported as 'saved'" (Major) — traced to the `MemoryStore` fallback contradicting its own boot-time banner, filed as follow-up #143; and the dirty-flag frame-cache race (Minor, shared with kimi and gemini), sent to a fixes round rather than patched in place | ~0.5 min |
| engine · batch 11 diff (save state) | kimi | 2 | 1 | 1 | 0 | "the indicator flashes unsaved for up to a frame after every successful save" — traced the same `sync_dirty_mirror`/async-put race codex and gemini found independently, three-way agreement; policy: the outside-project-root save reporting a false `saved` is real but pre-existing (`is_pending()` had the same gap) and filed as #143 rather than fixed in this batch — kimi's own note said so first | ~6 min |
| engine · batch 11 diff (save state) | gemini | 3 | 1 | 0 | 1 | first review in this effort (agy's ripgrep loader warning did not block a full read) — "Frame-boundary desynchronization" (Major), the third independent trace of the dirty-flag race, rated highest of the three for the `beforeunload`/background-tab-throttle angle; rebutted: the stranded-masks-active-saving priority (Major) is exactly what the settled plan's own bullet specifies, not an executor deviation — kept as specified on Jesse's ruling; the allocate-and-sort-per-poll note (Minor) rebutted as negligible | ~5 min |
| engine · batch 11 diff (save state) | claude (planner) | 1 | 1 | 0 | 0 | `insiculous_web/docs/roadmap.md` still said "the save status is what is left" — this batch ships it; the doc-only fix landed in the site commit rather than duplicated across both review files | — |
| site · batch 11 diff (save state) | codex | 2 | 2 | 0 | 0 | "Unsaved script edits still display 'Saved in this browser'" (Major) — `scriptsPanel.isDirty()` already exists in the same file for exactly this reason and was not wired in; and "The debounce timer announces saves that have already completed" (Minor, shared with kimi), both fixed by the planner | ~0.5 min |
| site · batch 11 diff (save state) | kimi | 4 | 0 | 2 | 2 | "reason can render with a dangling em dash" — false: every `Failed`-state construction in `persist/mod.rs` always sets a non-empty formatted reason, no path produces an empty one; "the banner and the live region announce the same failure twice" — overstated: verified the two surfaces track different failure classes (dispatch refusal vs. persist stranding) and rarely fire for the same event; the debounce race (with codex, accepted) and the load-time announcement noise (accepted, filed as web#62 rather than bundled into this round's two approved fixes) | ~5 min |
| site · batch 11 diff (save state) | claude (planner) | 0 | 0 | 0 | 0 | no new findings against the site diff; the one defect found this round (`docs/roadmap.md`) is filed under the engine round since the doc lives in this repo but the code that motivated it lives there | — |
| engine · batch 11 fix delta (history-only dirty flag) | codex | 0 | 0 | 0 | 0 | "No actionable findings" — the moved `Game` impl unchanged, the history-only flag preserves failure/pending handling while fixing the stale indicator | ~1.7 min |
| engine · batch 11 fix delta (history-only dirty flag) | kimi | 2 | 2 | 0 | 0 | "the page's switch/import/reset confirmation reads the same stale combined flag the PR fixes for the indicator" (rated Minor, "safe-direction residue") — third independent trace of the same race, now in `playground_is_dirty()`; also `CLAUDE.md`'s `EditorRunOptions` field inventory left at seven after the diff added an eighth | ~7.1 min |
| engine · batch 11 fix delta (history-only dirty flag) | gemini | 3 | 3 | 0 | 0 | "Frame-boundary desynchronization" (Major, blocking) — the same `playground_is_dirty()` staleness kimi found, rated higher: a phantom "unsaved changes" prompt could persist indefinitely on an unfocused tab; disagreement with kimi's severity sent to Jesse, who ruled extend now — also the same `CLAUDE.md` field-count gap, and a `CLAUDE.md` sentence conflating the atomic `dirty_flag` with the title bar's actual source (`EditorContext.is_dirty`) | ~6.5 min |
| engine · batch 11 fix delta (history-only dirty flag) | claude (planner) | 1 | 1 | 0 | 0 | the File Map's `editor_game/mod.rs` entry still claimed "struct and Game impl" after this round's own production-scope split moved the `Game` impl to the new `game_impl.rs`, which had no File Map entry at all | — |
